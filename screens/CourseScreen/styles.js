import { StyleSheet } from 'react-native';
import { white, darkBlue } from '../../constants';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: darkBlue,
    padding: 20,
  },
  text: {
    color: white,
  },
});

export { styles };
